{
  "name": "temp-email-api",
  "version": "4.0.0",
  "description": "MUNAX MAIL — stateless temp email API with peak aura dashboard",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
